# 骑宠恶魔跳跃当前状态备份（2026-05-06）

## 1. 文档目的

这份文档用于给 `SuperSkillWnd` 当前骑宠恶魔跳跃实现做一次**可追溯快照**，明确：

- 当前到底部署的是哪一版
- 已经打通到什么程度
- 证据是什么
- 还剩下什么问题
- 下一轮修复不该再回退哪些链路

本说明描述的是 **2026-05-06 当前可运行快照**，不是最终完结版。

## 2. 快照信息

- 日期：`2026-05-06`
- 仓库：`G:\code\c++\SuperSkillWnd`
- 当前分支：`backup/20260506-mounted-demonjump-status`
- 当前源码 marker：`v22.45-2026-05-06-mounted-up-side-wing7-native-tail-reset`
- 上一次已入库备份提交：`c51e656 backup: snapshot mounted demon jump status 2026-05-06`

## 3. 本次快照对应的哈希证据

- 构建产物：`G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
- 运行目录：`G:\code\mxd\Data\Plugins\SS\SS.dll`
- 当前 SHA256：
  - `349B1A740E2206C2AEACA859515DB25D4228E91DBDBE08B9DEE68BF630E1D232`

说明：

1. 当前构建产物与运行目录 DLL 哈希一致。
2. 说明这份快照对应的运行 DLL 已经覆盖到游戏目录。
3. 后续判断版本时仍以 `C:\SuperSkillWnd.log` 的 `[Build] marker=...` 为准，哈希只作为交叉校验。

## 4. 当前涉及的核心文件

- `src/dllmain.cpp`
- `src/runtime/dllmain_section_runtime_hooks.inl`
- `src/skill/skill_overlay_bridge.cpp`

说明：

- 这 3 个文件共同构成当前骑宠恶魔跳跃可运行状态。
- 本轮不应把无关的 `src/ui/*` 或 `auth_system/*` 脏改动混入骑宠恶魔跳跃备份。

## 5. 当前已确认打通的能力

截至本次快照，骑宠 `1932065` 配置 `30010110` 后，以下链路已经实测打通：

1. 骑宠场景可以走 `30010110` 根技能，而不是落回“骑乘中无法使用”或错误职业链路。
2. `↑↑` 上跳子技能 `30010183` 可以进入原生 release / packet / visual / tail 清理链。
3. `←←` / `→→` 侧跳子技能 `30010184` 可以进入同一套 mounted demon jump 子技能链。
4. 按住跳跃的滑翔子技能 `30010186` 已经能进入 mounted glide 链，不再是完全无效。
5. 之前反复出现的这些问题，在当前快照里已经不再是主问题：
   - “骑乘中无法使用”
   - “当前能力值不足，不能使用装备”
   - “无法在大乱斗中使用的技能”
   - 二次触发长期失效
   - 直接跳一下就崩溃

## 6. 当前日志证据

### 6.1 版本证据

`C:\SuperSkillWnd.log` 已记录：

- `[Build] marker=v22.45-2026-05-06-mounted-up-side-wing7-native-tail-reset`

### 6.2 上跳 `30010183` 证据

日志已出现：

- `B28A00 ... skill=30010183 sent=30010183 mount=1932065 ... result=1`
- `MountDemonJumpNativeTail reset ... mode=exact353 ... afb=353->-1`

说明：

- 上跳 release 封包链已成功送达。
- `353` native tail reset 已经落地，不再停留在旧的残留状态。

### 6.3 左右跳 `30010184` 证据

日志已出现：

- `B28A00 ... skill=30010184 sent=30010184 mount=1932065 ... result=1`
- `MountDemonJumpNativeTail reset ... mode=exact354 ... afb=354->-1`
- 并且已覆盖 `4A4=0x00000007` 场景

说明：

- 这证明左右跳不是“体感偶发”，而是已经进到 `433FEC` 后置 tail 清理。
- `v22.45` 的关键价值，就是把原来只放行 `exact353` 的 native tail reset，安全扩到 `exact354`。

### 6.4 滑翔 `30010186` 证据

日志已出现：

- `B28A00 ... skill=30010186 sent=30010186 mount=1932065 ... result=1`
- `ABAF70 keep child visual mount=1932065 skill=30010186`
- `MountDemonJumpTailGate clear ... caller=0x00433FEC ... gate=0/1/3->0/0/0`
- `MountDemonJumpNativeTail skip ... afb=362 state1172=-1 state1192=362`

说明：

1. 滑翔当前已经不是“完全没发出去”。
2. 当前策略是保留 `30010186` 自己的 child visual，不再错误改回别的技能。
3. 但 `362` 这条 native action tail 仍然没有被强制 reset，这也是后面姿态问题仍残留的直接线索。

## 7. 当前剩余主问题

当前只保留 1 个明确主问题：

1. 开始滑翔后，控制表现是站立姿态；约 1 秒后会闪一下变成跳跃姿态；再过约 1 秒才会完全进入跳跃姿态。目标是让滑翔从开始到结束都保持跳跃姿态。

补充判断：

- 这个问题的性质已经不是“技能未启用”。
- 也不是“封包没发出去”。
- 也不是“根技能 / 子技能映射错了”。
- 当前更像是 `30010186 / afb=362` 的原生动作态、展示态、tail 清理节奏之间没有完全同步。

## 8. 当前稳定结论

后续继续修这个问题时，下面这些结论不要再推翻重试：

1. `30010110` 在骑宠态必须继续走 root skill + child skill 的 mounted runtime 路线，不能退回职业原生直放。
2. `30010183 / 30010184 / 30010186` 都必须保留 child release / child visual 识别。
3. `30010184` 的问题已经证明需要 `exact354` native tail reset，不能再退回只处理 `353`。
4. `30010186` 不能直接照抄 `353/354` 的 reset 方式硬清，因为当前日志明确显示它跑的是 `afb=362`。
5. 版本确认以后优先看日志 marker，不用文件时间判断“是不是最新 DLL”。

## 9. 下一轮修复边界

下一轮如果继续修滑翔姿态，只建议围绕下面这个边界推进：

1. 保持当前 `30010183 / 30010184` 已打通链路不回退。
2. 不重开“能不能触发”“会不会提示不能使用”“会不会崩”的旧问题。
3. 重点看 `30010186 / afb=362` 对应的姿态保持和切换时机。
4. 优先关注“为什么滑翔开始时没有立刻维持 jump pose”，而不是继续重写 packet route。

## 10. Git 备份范围建议

做这次 GitHub 备份时，建议只纳入：

- `src/dllmain.cpp`
- `src/runtime/dllmain_section_runtime_hooks.inl`
- `src/skill/skill_overlay_bridge.cpp`
- `docs/16_骑宠恶魔跳跃当前状态备份_2026-05-06.md`
- `G:\code\c++\SuperSkillWnd\.claude\rules\CLAUDE.md`

不要顺手混入：

- `src/ui/retro_skill_panel.cpp`
- `src/ui/retro_skill_text_dwrite.cpp`
- `auth_system/*`
- 其他本轮未验证、与骑宠恶魔跳跃无关的脏改动

## 11. 2026-05-07 增量记录（v22.46）

### 11.1 本轮问题

基于回退后的 `v22.45` 现场日志，继续确认了两个残留问题其实同源：

1. 左右恶魔跳跃（`30010184`）结束后，落地会有约半秒不能动。
2. 滑翔（`30010186`）结束后，短时间内无法再次触发恶魔跳跃。

### 11.2 本轮修点

1. `433380-pass-terminal` 终态清理时，除了 demon jump transient state，再额外清掉：
   - `TryForceClearMountedSoaringFlightActiveForMount(..., "433380-pass-terminal")`
2. `HasMountedDemonJumpLateSustainIntent(...)` 收紧为：
   - 有真实 recent intent 时继续放行；
   - `30010184` 只接受最近 child cache 的短窗口；
   - `30010186` 只接受最近 glide packet 或最近 child cache 的短窗口；
   - 不再因为 `currentSkillId == 30010184/30010186` 就无限续命。

### 11.3 预期效果

1. 左右恶魔跳跃结束后，本地飞行活动态应更快退出，减少落地后半秒不能动。
2. 滑翔结束后，`30010186` 不再靠 stale current context 挂着，应能更快重新触发恶魔跳跃。

## 12. 2026-05-07 增量记录（v22.47）

### 12.1 本轮问题

`v22.46` 的日志已经把剩余问题继续缩小：

1. `433380-pass-terminal` 已能清掉 mounted flight active。
2. 但滑翔结束后的快速重触发，仍会被
   `B28A00 suppress repeated glide`
   挡住。
3. 关键日志年龄集中在 `16ms / 31ms / 47ms`，说明当前 600ms 的重复滑翔抑制窗口明显过长。
4. 旧逻辑在“被 suppress 的重复包”分支里还会再次 `RememberMountedDemonJumpGlidePacket(...)`，这会把抑制窗口不断往后推，形成“越按越触发不出来”的体感。

### 12.2 本轮修点

1. 保留 glide late sustain 用的窗口：
   - `kMountedDemonJumpGlidePacketSustainMaxAgeMs = 600`
2. 新增纯 dedupe 用窗口：
   - `kMountedDemonJumpRepeatedGlidePacketSuppressMaxAgeMs = 80`
3. `B28A00` 的 `30010186` 重复包抑制改为只使用 80ms 去重窗口。
4. 在 `B28A00 suppress repeated glide` 分支里，不再重复刷新 `RememberMountedDemonJumpGlidePacket(...)`，避免 suppress 自己不断延长自己的阻塞时间。

### 12.3 构建与证据

1. 新 build marker：
   - `v22.47-2026-05-07-mounted-glide-retrigger-window-tighten`
2. 新编译产物 SHA256：
   - `6DA67776A62073726652C86AF9E5C705408B176563479F50FE8E9283BD0C33D4`
3. 本次编译已成功。
4. 运行时 `G:\code\mxd\Data\Plugins\SS\SS.dll` 在记录时仍被 `Maplestory` 进程占用，尚未完成覆盖；当时运行时 DLL 仍是上一版：
   - `B9BCFAF473FEA45355C0D6FE4A1789AB5FD374EC3473980ED9B5EFA9D5FE3406`

### 12.4 预期效果

1. 滑翔结束后，再次触发恶魔跳跃的等待窗口应从“接近半秒级/可被连续按键继续推迟”缩到“仅保留一次输入去重需要的极短窗口”。
2. 连续补按时，不会再因为 suppress 分支反复刷新 glide tick 而把自己继续锁住。

## 13. 2026-05-07 增量记录（v22.48）

### 13.1 本轮问题

`v22.47` 仍然保留了 glide 结束后 `root/current=30010186` 持续挂住的现象，所以只缩小重复包窗口还不够。

### 13.2 本轮修点

1. 在 `B1DB10` / `B1C9E0` 晚期移动收尾里，若当前有效 child 仍是 `30010186`，再补一次：
   - `TryForceFinalizeMountedDemonJumpUpStateAfterPacket(..., 30010186, ...)`
2. 让 glide 的最终收束不再只依赖 `B28A00` 发包瞬间，而是等晚期移动链真正把 gate / lock 归零后再清 root/current/context。
3. `TryForceFinalizeMountedDemonJumpUpStateAfterPacket(...)` 对 `30010186` 也顺手清掉 mounted soaring active，避免 glide 结束后活动态残留。

### 13.3 构建与证据

1. 新 build marker：
   - `v22.48-2026-05-07-mounted-glide-late-finalize`
2. 新编译产物 SHA256：
   - `B1D8FBBF62218C4DE4ACAA0062284B8D98AFB54F8CC42595704F2FEEBF5283C2`
3. 当前运行时 `G:\code\mxd\Data\Plugins\SS\SS.dll` 仍是上一版 `v22.47`：
   - `6DA67776A62073726652C86AF9E5C705408B176563479F50FE8E9283BD0C33D4`
4. `Maplestory` 进程当前仍占用该 DLL，导致尚未完成覆盖部署。

### 13.4 预期效果

1. glide 结束后，root/current/context 应在晚期移动链收束时更早清掉。
2. 用户应更快重新触发 `30010186`，而不是再等 terminal clear 兜底。

## 14. 2026-05-07 增量记录（v22.49）

### 14.1 本轮问题

`v22.48` 最新日志显示：

1. 当前确实已经跑到 `v22.48`，不是部署问题。
2. 但 glide 链里仍会出现：
   - `B2F370 arm missing intent ... source=keep-child`
   - 随后的 `B28A00 suppress repeated glide ... age=31`
3. 这说明旧的 child keep-child 回路仍在“没有 fresh intent 的情况下”把 glide 自己再次喂回去，造成极短时间内的自触发与重复包抑制。

### 14.2 本轮修点

## 15. 2026-05-09 增量记录（v23.44）

### 15.1 本轮问题来源

基于 2026-05-09 新日志：

- `C:\SuperSkillWnd失败+成功.log`
- `C:\SuperSkillWnd首次跳跃 卡顿.log`
- `C:\SuperSkillWnd 首次恶魔跳跃 失败.log`
- `C:\SuperSkillWnd 失败2.log`
- `C:\SuperSkillWnd失败3.log`

继续确认了当前 `↑↑` 失败至少有两类：

1. 一类会进入 `575D60 / B22630`，但首个 mounted up 路径在 root-only context 下推进得过慢，需要后续 fresh child 才能继续。
2. 另一类纯失败样本停在 `B844D0-up-seed -> 433380 pass clear -> 7DBC50 remember child`，连 `575D60 / B22630` 都没有进。

### 15.2 当前稳定日志结论

1. `失败+成功.log` 显示：
   - `575D60 preseed ... probeChild=30010183`
   - 但首个 `0x01480000` 前仍可能只有 `root/current=30010110/30010110`
   - 后续才出现 `remember source=7DBC50 mount=1932065 child=30010183`
2. `失败.log / 失败2.log / 失败3.log` 显示：
   - 纯失败样本会停在 `7DBC50 remember child`
   - 没有继续跑到 `575D60 / B22630 / B2F370`

这说明：

1. up-gate probe 里已经知道目标 child 是 `30010183`。
2. 但 short child cache 与 narrow recent-intent 在首跳慢路径上仍可能错过。

### 15.3 本轮修点

1. `HasMountedDemonJumpContextPrimedForMount()`
   - 当 recent child cache 已经过短窗口，但 up gate-probe 仍新鲜时，新增：
     - `promote probe child ... -> child=30010183`
   - 只对 mounted demon up-child 生效，不扩到 side/glide。
2. `ObserveMountedDemonJumpFreshChildLevelLookup()`
   - 当 `7DBC50` 已经拿到 fresh up-child，且当前处于 `resolve-active` 导致不能正常 rearm 时：
     - 只刷新窄 demon recent-intent tick
     - 不在 `7DBC50` 里做额外强 prime，避免把 resolve 路径再放大

### 15.4 构建与哈希证据

1. 新 build marker：
   - `v23.44-2026-05-09-mounted-up-gateprobe-child-refresh`
2. `cmd /c build.bat`：通过
3. SHA256：
   - `G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   - 两者一致：
     - `C57257F0F61D0116FE7508032E7EB6FF5E275DA3F1A782769B91140294D57613`

### 15.5 当前状态

1. 代码与运行目录 DLL 已更新到 `v23.44`。
2. 这轮还没有新的用户实测结论；下一轮应优先看：
   - 首次 `↑↑` 失败样本是否仍停在 `7DBC50 remember child`
   - `575D60 / B22630` 是否更稳定进入
   - `C:\SuperSkillWnd.log` 中是否已经出现新的 `v23.44` marker

1. 在 `hkSkillReleaseClassifierB2F370Dispatch(...)` 里增加 guard：
   - 如果 `localSkillSource == "keep-child"` 且当前没有 fresh intent，就不再执行 `B2F370-arm-missing-intent` 那条 synthetic rearm。
2. 新增日志：
   - `B2F370 skip stale keep-child rearm ...`
3. 目标是直接切断 glide 的 stale keep-child 自激回环，而不是继续放大/缩小去重窗口来硬压症状。

### 14.3 构建与证据

1. 新 build marker：
   - `v22.49-2026-05-07-mounted-glide-keepchild-rearm-guard`
2. 新编译产物 SHA256：
   - `41F2F932A22534C5B3BF6120A61DFC5BA575775A52F99E27F61DCB6024DC25E1`
3. 运行时 DLL 已完成覆盖，当前运行时 SHA256 同样为：
   - `41F2F932A22534C5B3BF6120A61DFC5BA575775A52F99E27F61DCB6024DC25E1`

### 14.4 预期效果

1. `source=keep-child` 的 stale glide 不再自己补 recent intent。
2. `B28A00 suppress repeated glide` 应显著减少，或至少不再由这条 stale keep-child 回路持续触发。

## 15. 2026-05-07 增量记录（v22.50）

### 15.1 本轮问题

`v22.49` 最新日志已经证实回归：

1. `B2F370 skip stale keep-child rearm ...` 不只出现在滑翔 `30010186`。
2. 同样出现在上跳 `30010183`、左右跳 `30010184`。
3. 对左右跳现场，日志还能看到：
   - `B2F370 local skill observed=30010184 ... source=keep-child`
   - `B2F370 skip stale keep-child rearm ... local=30010184`
   - 随后 `7CF270 ... hasDoubleIntent=0 hasDemonIntent=0`
4. 这说明 `v22.49` 的 guard 过宽，把非滑翔子技能本来应该补上的 demon intent 也挡掉了，所以左右恶魔跳跃会直接失效。

### 15.2 本轮修点

1. 把 `hkSkillReleaseClassifierB2F370Dispatch(...)` 里的 `staleKeepChildWithoutFreshIntent` 收窄为：
   - 仅 `localSkillId == 30010186`
   - 且 `source == keep-child`
2. 这样：
   - `30010186` 仍继续拦 stale glide keep-child rearm；
   - `30010183 / 30010184` 恢复允许 `B2F370-arm-missing-intent` 补 demon intent。

### 15.3 构建与证据

1. 新 build marker：
   - `v22.50-2026-05-07-mounted-keepchild-guard-glide-only`
2. 新编译产物 SHA256：
   - `B9664161D539EFB150B6F15C81E8668B27E7EC35B2A1386C52E8296C941D8FD2`
3. 运行时 DLL 已完成覆盖，当前运行时 SHA256 同样为：
   - `B9664161D539EFB150B6F15C81E8668B27E7EC35B2A1386C52E8296C941D8FD2`

### 15.4 预期效果

1. 左右恶魔跳跃 `30010184` 不再被 `v22.49` 的 stale keep-child guard 误伤。
2. 滑翔 `30010186` 仍保留单独的 stale keep-child 防回环保护。

## 16. 2026-05-07 增量记录（v22.51）

### 16.1 本轮问题

`v22.50` 最新日志已经把 glide 的剩余问题继续缩小：

1. 左右恶魔跳跃已经恢复，日志可见：
   - `B2F370 arm missing intent ... local=30010184`
   - `hasDemonIntent=1`
2. 但 glide 结束后，后续一段时间内重新触发恶魔跳跃仍会失效。
3. 关键证据不是 `root/current` 没清，而是 glide 清理后新输入仍卡在 root：
   - `B22630 ... root=30010110 current=30010110`
   - `B1DB10 ... result=0`
4. 同一段日志里还能看到：
   - `433380-tail-native-action ... skip ... afb=362 state1192=362`
5. 这说明 glide 的原生 tail/action 状态 `362` 没有被纳入 reset 条件，root/current 清掉后，native tail 仍残留，继续挡住后续 fresh trigger。
6. 同时还确认了一个硬 bug：
   - `TryForceFinalizeMountedDemonJumpUpStateAfterPacket(...)` 的入口仍只放行 `30010183/30010184`
   - 外层虽然已经开始把 `30010186` 传进去，但 helper 自己会直接拒绝，导致 glide finalize 实际从未生效。

### 16.2 本轮修点

1. `TryForceFinalizeMountedDemonJumpUpStateAfterPacket(...)` 入口正式放行 `30010186`。
2. `TryForceFinalizeMountedDemonJumpNativeActionTailState(...)` 新增 `exact362`：
   - `afb == 362`
   - `state1172 == -1`
   - `state1192 == 362`
3. glide 在清完 post-packet tail slot 后，也会走 native-action tail finalize。
4. 这样可以把之前日志里残留的：
   - `afb=362`
   - `state1192=362`
   直接收掉，避免 glide 结束后继续卡住后续恶魔跳跃输入。

### 16.3 构建与证据

1. 新 build marker：
   - `v22.51-2026-05-07-mounted-glide-362-tail-finalize`
2. 新编译产物 SHA256：
   - `3B87B1FBEBDCD6D9994BBF9F32209A0E1054A6F51D49E40043A5CA0BEE996D3C`
3. 当前运行时 DLL 仍被游戏进程占用，尚未完成覆盖；运行时 SHA256 仍是上一版：
   - `B9664161D539EFB150B6F15C81E8668B27E7EC35B2A1386C52E8296C941D8FD2`

### 16.4 预期效果

1. glide 结束后不再残留 `362` native tail/action 状态。
2. 后续新的恶魔跳跃输入不应再卡在 `root/current=30010110 -> result=0` 的空转状态。

## 17. 2026-05-07 增量记录（v22.52）

### 17.1 本轮问题

`v22.51` 运行时证据继续收窄出新的误路由点：

1. `v22.51` 已经确认生效：
   - marker=`v22.51-2026-05-07-mounted-glide-362-tail-finalize`
   - 运行时 SHA256=`3B87B1FBEBDCD6D9994BBF9F32209A0E1054A6F51D49E40043A5CA0BEE996D3C`
2. glide 的 `362` native tail 已经被成功清掉：
   - `MountDemonJumpNativeTail reset ... mode=exact362 afb=362->-1 state1192=362->-1`
3. 但 glide 结束后的下一次触发，仍会出现：
   - `root=30010110 current=30010110`
   - `B300AC root->child reroute ... source=proxy-23001002 ... child=30010186`
4. 随后日志直接掉进：
   - `hasDemonIntent=0`
   - `finalGuard[busy=1 ... age=0]`
   - `B1DB10 ... result=0`
5. 这说明问题已经不再是 `362` 没清，而是 glide 结束后，`B300AC` 仍会在 context 只有 root、没有 child 的情况下，用 `proxy-23001002` 的 fallback 把下一次触发强行改回 `30010186`。

### 17.2 本轮修点

1. 在 `B300AC` 的 demon root->child reroute 里新增条件：
   - 只有 `hasMountedDemonContext == true`
   - 且 `demonContextSkillId` 本身已经是 child skill
   - 才允许 `proxy-23001002 -> 30010186` 这条 fallback 生效。
2. 如果当前 context 只是：
   - `root=30010110`
   - `current=30010110`
   则不再把它硬改成 `30010186`。
3. 目标是把 `proxy-23001002` fallback 收窄回“真实 glide child context 仍在”的场景，避免 glide 结束后的下一次 fresh trigger 被误路由回旧 glide child。

### 17.3 构建与证据

1. 新 build marker：
   - `v22.52-2026-05-07-mounted-glide-proxy-fallback-child-context`
2. 新编译产物 SHA256：
   - `EB5FCCEC3A262AEED64ECA587537B806AA789167659797833AC02D0C16E9D4D3`
3. 运行时 DLL 已完成覆盖，当前运行时 SHA256 同样为：
   - `EB5FCCEC3A262AEED64ECA587537B806AA789167659797833AC02D0C16E9D4D3`

### 17.4 预期效果

1. glide 结束后，下一次触发不再在 `root/current=30010110` 的 context 下被误改回 `30010186`。
2. 新输入应重新走真实 child 解析，而不是被 stale `proxy-23001002` fallback 劫持。

## 18. 2026-05-07 增量记录（v22.53）

### 18.1 本轮问题

基于当前最新 `C:\SuperSkillWnd.log`，又把两个残留现象进一步钉死成了两条明确链路：

1. 左右恶魔跳跃 `30010184` 落地后仍会有半秒左右不能动。
2. 滑翔结束后，立刻再按新的恶魔跳跃输入，仍会被当成旧 glide child，导致无法重新触发。

### 18.2 日志证据

1. `30010184` 的下降尾态与 `30010183` 现在是同构的：
   - `B1DB10-tail ... current=30010184 gate=3/0/1->3/0/1`
   - 本地尾态同时停在 `gate=0/1/3 slot=0x01C9EAFE`
2. 但当前代码只对 `30010183` 执行 `descending-up` 提前清 gate，所以 `30010184` 只能继续等到：
   - `433380 terminal clear`
   - `MountFlightState clear active reason=433380-pass-terminal age=343/422`
3. 滑翔复触发失败链已经完整可见：
   - `recent=1 root=30010110 current=30010186`
   - `B22630 ignore stale child ... cached=30010186`
   - `B2F370 skip stale keep-child rearm ... local=30010186`
   - `hasDemonIntent=0`
4. 这说明“滑翔结束后新输入失效”不是没进链，而是晚期 sustain 还把新输入误判成旧 glide child。

### 18.3 本轮修点

1. 新增 `IsMountedSoaringFlightActiveForMount(...)`。
2. `HasMountedDemonJumpLateSustainIntent(...)` 对 `30010186` 的 600ms glide sustain 收紧为：
   - 既要命中最近 glide packet，
   - 也要当前 mounted flight active 仍然活着。
3. `TryForceClearMountedDemonJumpDescendingUpState(...)` 扩成通用下降尾态清理：
   - 现在 `30010183 / 30010184 / 30010186` 都能在 `gate=3/0/1` 且 local lock 已归零时提前清 user gate。
4. `B1DB10` / `B1C9E0` 增补 side/glide 晚期 finalize：
   - `30010184` 走 `side-late-finalize`
   - `30010186` 继续走 `glide-late-finalize`
5. 目标是：
   - side 不再硬等 `433380-pass-terminal` 才解身；
   - glide 在下降尾态一收住，recent/current 就不再继续把新输入当成旧 child。

### 18.4 构建与哈希证据

1. 新 build marker：
   - `v22.53-2026-05-07-mounted-descending-tail-glide-active-sustain`
2. 新编译产物 SHA256：
   - `2CF5BFB40740A6B5D6E13270603C1BE0196F02598814342FF686D375BA92DA71`
3. 运行时 DLL 已完成覆盖，当前运行时 SHA256 同样为：
   - `2CF5BFB40740A6B5D6E13270603C1BE0196F02598814342FF686D375BA92DA71`

### 18.5 待验证点

1. `30010184` 落地后是否还会有那段约半秒不能动。
2. 滑翔结束后，是否能立刻重新触发新的恶魔跳跃输入。
3. 重启游戏后，`C:\SuperSkillWnd.log` 是否出现：
   - `marker=v22.53-2026-05-07-mounted-descending-tail-glide-active-sustain`

## 19. 2026-05-09 增量记录（v23.22）

### 19.1 本轮问题

用户本轮提供的交替样本：

- `C:\SuperSkillWnd失败-成功-失败-成功-失败-成功.log`

已经确认仍然是旧版：

- `marker=v23.20-2026-05-09-mounted-job100-gateprobe-seed-refresh`

这份日志把当前 `↑↑` 不稳定问题进一步钉死成了一个更早的缺口：

1. `42DE20 caller=0x00ADF02B` 已经先把 mounted demon jump gate-probe arm 起来。
2. 紧接着 `7DBC50 caller=0x00A04B7A` 会进入一串 mounted source query：
   - `80001099`
   - `2301014`
   - `80001095`
   - `30001902`
3. 但这些 query 的 cache preseed 仍然都是：
   - `entrySkill=30010110`
   - `freshChild=0`
4. 说明当时缺的不是 “有没有 probe / 有没有 arm”，而是：
   - **gate-probe 已经成立，但还没有把“这次应该预种哪个 child”带进 cache entry。**

### 19.2 本轮修点

本轮只改最小一层，不碰 `B300AC / B28A00` 后链：

1. gate-probe 运行态除了继续记录：
   - `mountItemId`
   - `tick`
2. 现在额外记录：
   - `preferredChildSkillId`
3. 对下列已确认属于 mounted `↑↑` 起跳链的 arm reason：
   - `42DE20`
   - `42DE20-no-intent`
   - `B22630-raw-up-commit`
   若当前坐骑允许 `30010183`，则 gate-probe 会带上：
   - `preferredChildSkillId = 30010183`
4. `ResolveMountedDemonJumpCacheEntrySkillId(...)` 在 recent-child fallback 之后、root fallback 之前，新增：
   - 读取 gate-probe preferred child
   - 若 mount 匹配且 child 合法，则直接把 cache entry 预种成 `30010183`

### 19.3 这次修法与 v23.21 的关系

`v23.21` 已经补了：

- `up-latch -> 30010183`

但老失败日志说明：

1. `A04B7A` 那个 gate-probe 时机，有可能还没形成可读的 `up-latch`
2. 所以只靠 `up-latch` 仍可能不够早
3. `v23.22` 进一步把 “42DE20 已 arm 的 up-child 偏好” 直接前传到 `A04B7A / 79AEB002` 预种阶段

### 19.4 构建与哈希证据

1. 新 build marker：
   - `v23.22-2026-05-09-mounted-job100-gateprobe-preferred-up-child`
2. 新编译产物 SHA256：
   - `E42FA88EF44B34C6BB6FA52CB924F4E9395C60659A16F107CE67E34EC1B007D0`
3. 运行时 DLL 已完成覆盖，当前运行时 SHA256 同样为：
   - `E42FA88EF44B34C6BB6FA52CB924F4E9395C60659A16F107CE67E34EC1B007D0`

### 19.5 下一轮验证重点

重启游戏后，只测一轮：

- 骑宠 `1932065`
- `↑↑`

优先确认新日志是否出现：

1. `[Build] marker=v23.22-2026-05-09-mounted-job100-gateprobe-preferred-up-child`
2. `caller=0x00A04B7A`
3. `stage=preseed query=80001099 ... entrySkill=30010183`

如果这里已经变成 `30010183`，说明：

- 旧版 `v23.20` 最早的缺口已经被补上
- 后续再看是否还存在更晚的 stale rearm / consume 问题

## 20. 2026-05-09 增量记录（`C:\SuperSkillWnd卡顿-失败-成功-失败-失败-滑翔起来了-成功.log`）

### 20.1 这份日志对应的结论

- 这份日志仍是 `v23.39-2026-05-09-mounted-up-stale-flight-clear` 运行态。
- 本轮把 `B844D0` 的 stale active 清理前移后，首跳前的旧 active 确实能先清掉，再进入 seed。
- 但“首次跳跃卡顿”还没有被完全消掉，说明问题不只在 `B844D0` 首拍清理时机。

### 20.2 按日志顺序记录的测试结果

1. `B844D0` 首拍先命中：
   - `clear active reason=B844D0-up-seed-stale-active mount=1932065 age=593 duration=593`
   - 说明旧 active 的确先被清了。
2. 随后 `B844D0 early-up seed` 能正常立起来：
   - `prime=1 root=1 current=30010110`
   - 说明清理前移没有破坏 up seed。
3. 第一段 `↑↑` 仍然卡顿/失败：
   - `B1DB10-main key=56 active=1`
   - 说明首跳动作链仍会先看到旧态。
4. 后续下降收尾能进入清理：
   - `B1DB10-tail key=56 active=0`
   - `B1DB10-descending-up ... gate=3/0/1->0/0/0`
   - `MountFlightState clear active ... age=219 duration=219`
   - 说明下降尾态清理仍是有效的。
5. 后续再次 `↑↑` / `AFB710` / `B2F370` 链路可继续跑通：
   - `B22630 up probe ... result=4`
   - `AFB710 observe native path`
   - `B2F370 local skill observed=30010183`
   - 说明滑翔/上跳后续链路并未断死。

### 20.3 这轮更新后的稳定判断

1. `B844D0` 的 stale active 前移清理是有效的。
2. 首次 `↑↑` 的卡顿仍需继续查更早的输入/门闩点。
3. `B1DB10-descending-up` 的 active 清理仍要保留，不能回退。

## 21. 2026-05-09 增量记录（`v23.41`）

### 21.1 本轮修点

1. 只给骑宠 `30010110` 的 mounted cruise warmup 收窄到 `40ms`。
2. 其余 mount 仍保持原来的 `120ms` warmup，不扩大影响面。
3. 目的还是首跳不再明显卡顿，但不改动别的骑宠飞行节奏。

### 21.2 构建与部署

1. 新 build marker：
   - `v23.41-2026-05-09-mounted-up-stale-flight-clear-early-cruise40`
2. 新编译产物 SHA256：
   - `BF4AFE6982E68DF24CF5A54CB83951DD1CF01530B6BDC9ED242F9BE567EA50F5`
3. 运行目录 DLL SHA256：
   - `BF4AFE6982E68DF24CF5A54CB83951DD1CF01530B6BDC9ED242F9BE567EA50F5`

### 21.3 待验证点

1. 重启后只测一轮 `骑宠 1932065 + ↑↑`。
2. 重点看首跳是否还会卡顿。
3. 如果卡顿仍在，再继续往 `B1DB10` / `7BECF0` 的更早门闩查。

## 22. 2026-05-09 增量记录（v23.42）

### 22.1 本轮问题

`v23.41` 最新日志继续稳定复现了同一条首跳缺口：

1. `B844D0` / `575D60` 已经能把骑宠 `1932065` 的 `30010183` 预种进 cache，并进入 early-up seed。
2. 但到了首个 `B1DB10` / `7BECF0` 主拍时，raw context 仍会回落成：
   - `root/current=30010110/30010110`
3. 同一轮里可以继续看到：
   - `B22630 up probe ... result=38 ... current=30010110`
   - 随后才变成
   - `B22630 up probe ... result=4 ... current=30010183`
4. 这说明首跳不是完全没 arm，而是 `B1DB10` 下降尾态清理看见的还是 raw root/current，没吃到已经 prime 好的 effective child 形态，导致首拍仍会拖一段。

### 22.2 本轮修点

1. `B1DB10` 离开时，`descending-up` 清理不再直接拿 `ShouldObserveMountedDemonJumpLatePath(...)` 提供的 raw `root/current`。
2. 现在先补读一次：
   - `TryReadMountedDemonJumpEffectiveContextState(mountItemId, ...)`
3. 再把这组 effective `root/current` 传给：
   - `TryForceClearMountedDemonJumpDescendingUpState(...)`
4. 目的不是扩大整条 mounted demon jump 路线，而是只修正这一拍：
   - cache / recent intent 已经确认是 `30010183`
   - 但 raw context 还没跟上
   - 导致 `B1DB10` 尾态清理慢半拍的问题

### 22.3 构建与哈希证据

1. 新 build marker：
   - `v23.42-2026-05-09-mounted-up-effective-descending-clear`
2. 新编译产物 SHA256：
   - `67CC5E2215B8B22540EB985F13433978C60860F09539729C8C62897974D9AFC4`
3. 运行目录 DLL SHA256：
   - `67CC5E2215B8B22540EB985F13433978C60860F09539729C8C62897974D9AFC4`
4. 构建已通过，运行目录已完成覆盖。

### 22.4 待验证点

1. 重启后只测一轮：
   - 骑宠 `1932065`
   - `↑↑`
2. 重点看三件事：
   - 首次跳跃是否还会卡顿
   - `↑↑` 是否仍然要失败多次才成功
   - 首次成功后的落地是否还残留那段迟滞
3. 日志侧优先确认是否更早出现：
   - `MountDemonJumpDescending clear reason=B1DB10-descending-up`
   - 并且这次发生在首轮 `30010183` 链里，而不是要等后续补按后才出现

## 23. 2026-05-09 增量记录（v23.45）

### 23.1 本轮新证据

基于最新日志：

- `C:\SuperSkillWnd尝试滑翔 滑翔变成了 ↑↑.log`
- `C:\SuperSkillWnd人物职业3001 真实滑翔.log`

确认骑宠滑翔误判的最早硬证据是：

1. `observed=23001002`
2. `B300AC root->child reroute`
3. `source=recent-child-cache`
4. `child=30010183`

也就是这条组合把骑宠滑翔直接抢成了上跳子技能 `30010183`，而不是让它继续走自己的 `30010186` 滑翔链。

### 23.2 本轮修点

1. 在 `hkMountedUnknownSkillReleaseBranchTargetB300AC(...)` 里只收窄这一种组合：
   - `recentNativeSkillId == 23001002`
   - `rerouteDemonChildSkillId == 30010183`
   - `rerouteDemonChildSource == "recent-child-cache"`
2. 这类样本直接回退到原始 branch，不再强行 `root->child reroute` 成 `30010183`。
3. 不碰已成功的 `30010183 / 30010184 / 30010186` 直链。

### 23.3 构建与哈希证据

1. 新 build marker：
   - `v23.45-2026-05-09-mounted-glide-recentchild-defer`
2. 新编译产物 SHA256：
   - `573AEA31F5E59A302E8B0CA5D18F602CDBAFF53D1CF3B802A8D10A3A46F59693`
3. 运行目录 DLL 仍被 `Maplestory.exe` 占用，当前还没能覆盖：
   - `C:\SuperSkillWnd.log` 侧的运行 DLL SHA256 仍为旧值
   - `C57257F0F61D0116FE7508032E7EB6FF5E275DA3F1A782769B91140294D57613`
4. 需要先关闭客户端进程后再把新 DLL 覆盖到：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`

## 24. 2026-05-09 增量记录（v23.47）

### 24.1 本轮输入证据

本轮继续对照的旧日志样本：

- `C:\SuperSkillWnd 首次跳跃卡顿‘.log`
- `C:\SuperSkillWnd 多次↑↑失败后成功一次.log`
- `C:\SuperSkillWnd ↑↑失败.log`
- `C:\SuperSkillWnd ↑↑失败-成功.log`
- `C:\SuperSkillWnd 滑翔无效，控制双击跳跃变成滑翔.log`

这些日志都还是旧版 marker：

- `[Build] marker=v23.46-2026-05-09-mounted-glide-proxy86-guard`

### 24.2 旧日志确认出的两个硬问题

#### 24.2.1 `↑↑` 首拍仍吃到 raw root/current

旧日志里已经能看到：

1. `B844D0-up-seed` 已经记住了：
   - `child=30010183`
   - `resolve-safe ... root=30010110 current=30010183 primed=1`
2. 但首个真正进入 `575D60 / B22630 / B1DB10` 的样本仍然是：
   - `B22630 enter ... root=30010110 current=30010110`
3. 对应样本可见于：
   - `C:\SuperSkillWnd ↑↑失败.log`
   - `C:\SuperSkillWnd 滑翔无效，控制双击跳跃变成滑翔.log`

结论：

- 问题不是“完全没 prime”。
- 而是 prime 结果还没在首个 `B22630` 入口被 original call 吃到。
- 所以会出现：
  - 首次跳跃卡顿
  - `↑↑` 常常失败
  - 多按几次后才偶发成功

#### 24.2.2 滑翔代理 `23001002` 仍会在 `B26760` 被 stale `30010183` 劫持

旧日志里已经明确出现：

1. `B2F370 observe native release mount=1932065 skill=23001002`
2. `B300AC defer glide recent-child ... child=30010183`
3. 紧接着：
   - `B26760 ... rewrite=recent-child-cache pending93=1`

对应样本可见于：

- `C:\SuperSkillWnd 滑翔无效，控制双击跳跃变成滑翔.log`

结论：

- 仅在 `B300AC` defer glide 还不够。
- 因为后面的 `B26760` 仍然可能按 recent-child-cache 把 `30010183` 再写回去。
- 这就是“按住跳跃滑翔”会被误带成 `↑↑` 或双击跳跃异常进入滑翔态的直接日志证据。

### 24.3 本轮实际代码落点

本轮只继续收窄 `src/runtime/dllmain_section_runtime_hooks.inl`：

1. 新增 `HasFreshMountedRuntimeSkillNativeReleaseRaw(...)`
   - 用最底层 raw recent-native-release 状态兜底判断 `23001002`
   - 目的：即使 higher-level helper 没命中，也不让 `B26760` 把 stale `30010183` 写回 packet 93
2. 在 `hkMountedDemonJumpContextInputB22630(...)` 入口新增极窄 `enter preseed`
   - 只在：
     - `mountItemId > 0`
     - `recentIntent=1`
     - `beforeGateMode == 3`
     - `beforeDownLatch == 0`
     - raw `root/current` 仍是 `0/0` 或 `30010110/30010110`
     - recent gate probe child 明确是 `30010183`
   - 才会执行：
     - `ForceRefreshMountedDemonJumpContextSeed(..., "B22630-enter-up-preseed", ...)`
   - 目的：把 first `B22630` 看到的形态从 raw root 拉到真实的 up child 形态
3. 修正 `B844D0 early-up seed` 日志字段错位
   - 现在日志里的 `staleActive / armIntent / prime / root / current` 对应关系已恢复正确

### 24.4 当前构建与部署证据

1. 新 build marker：
   - `v23.47-2026-05-09-mounted-up-preseed-rawglide-guard`
2. 新编译产物 SHA256：
   - `6A87655DB437862F0D71F338170D685267DF6F333F59C7F136B4257FDD6F6B20`
3. 运行目录 DLL SHA256：
   - `6A87655DB437862F0D71F338170D685267DF6F333F59C7F136B4257FDD6F6B20`
4. 当前已确认：
   - `G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   - 两者哈希一致，运行目录已完成覆盖

### 24.5 本轮尚未拿到的新运行证据

这轮代码已经编译并覆盖，但**还没有**拿到 `v23.47-2026-05-09-mounted-up-preseed-rawglide-guard` 的新日志。

所以下一轮验证必须优先看这几个点：

1. `↑↑` 测试时，首个 `B22630 enter` 前后是否出现：
   - `B22630 enter preseed ... root=30010110 current=30010183`
2. 同一轮里，首个 `B22630 enter` 是否不再停在：
   - `root=30010110 current=30010110`
3. 滑翔测试时，`B26760` 是否不再出现：
   - `rewrite=recent-child-cache`
   - 且对应 child 仍是 `30010183`

### 24.6 当前结论

本轮不是泛化改动，也不是“再猜一次”：

1. `B22630-enter-up-preseed` 直接对应旧日志里“prime 成功但首个 `B22630` 仍读 raw root/current”的硬证据。
2. `HasFreshMountedRuntimeSkillNativeReleaseRaw(23001002)` 直接对应旧日志里“`B300AC` 已 defer glide，但 `B26760` 仍按 recent-child-cache 回写 `30010183`”的硬证据。
3. 是否真正修掉，还必须以后续 `v23.47` 新日志为准，不能再用旧 marker 的体感结论代替。

## 25. 2026-05-09 增量记录（v23.48）

### 25.1 本轮新日志证据

本轮直接对照运行中最新日志：

- `C:\SuperSkillWnd.log`
- marker：
  - `v23.48-2026-05-09-mounted-firstjump-7dbc50-throttle`

用户本轮反馈与日志证据一致：

1. “首次不会卡了”
2. “跳跃一次后一直在自动跳跃了”

### 25.2 v23.48 已确认修掉的点

`v23.48` 对 `7DBC50` 做了更窄的首跳节流后，首跳卡顿链已经明显收敛：

1. 旧版里会反复出现：
   - `[MountDemonJumpPrime] reason=7DBC50 ... primed=1`
2. 首次普通跳/首次进入 mounted up 链前，会被大量 `7DBC50` rearm 拖卡。
3. 用户本轮体感也确认：
   - 首次不会卡了

结论：

- `v23.48` 的“首跳不卡”不是体感误差，和节流 `7DBC50` 的代码改动一致。

### 25.3 v23.48 暴露出的新主问题

普通跳或 `↑↑` 释放结束后，demon 链被再次复燃，导致出现自动续跳/自动续滑翔。

日志里能直接看到：

1. `B28A00-up-postpacket` 已经执行清理：
   - `[MountDemonJump] clear bridge transient state mount=1932065 reason=B28A00-up-postpacket native=0`
   - `[MountDemonJump] clear runtime transient state mount=1932065 reason=B28A00-up-postpacket`
   - `[MountDemonJumpPostPacket] clear ... root=30010110/30010183->0/0`
2. 清理后白名单观察也显示 demon intent/route 已下去：
   - `[MountWhitelist] 7CF270 observe skill=30010183 native=0 mount=1932065 hasDoubleIntent=0 hasDemonIntent=0 routeDouble=0 routeDemon=0`
3. 但清理后立刻又出现新的 `7DBC50` child lookup：
   - `[MountDemonJump] 7DBC50 caller=0x00AE0479 query=30010183 lookup=30010183 flags=0 raw=0 final=1`
   - `[MountDemonJump] 7DBC50 caller=0x00433B4C query=30010186 lookup=30010186 flags=0 raw=0 final=1`

结论：

1. 自动续跳不是 `B28A00-up-postpacket` 完全没清。
2. 也不是单纯旧的 demon recent-intent / route-arm 没清。
3. 而是清理后仍有别的“新鲜触发源”允许 `7DBC50 fresh-child` 再次把 demon 链点燃。

### 25.4 v23.48 根因定位

继续对 runtime 代码和日志对照后，定位到残留源是 `gate probe`：

1. `ObserveMountedDemonJumpFreshChildLevelLookup(...)` 的准入条件不是只看 recent-intent。
2. 它允许：
   - `HasRecentMountedDemonJumpIntent(...) == false`
   - 但 `TryGetRecentMountedDemonJumpGateProbeMountItemId(...) == true`
   - 时仍然继续进入 fresh-child 续命逻辑。
3. `ClearMountedDemonJumpTransientRuntimeState(...)` 在 `v23.48` 之前会清：
   - demon recent-intent
   - native-release
   - native-child
   - postpacket visual
   - glide packet
   - rewrite packet
4. 但它不会清：
   - `g_recentMountedDemonJumpGateProbeItemId`
   - `g_recentMountedDemonJumpGateProbeChildSkillId`
   - `g_recentMountedDemonJumpGateProbeTick`

因此：

1. `B28A00-up-postpacket` 之后，intent/route-arm 虽然清了。
2. 旧的 gate probe 还活着。
3. 后续 `7DBC50` fresh-child lookup 只靠 probe 也能再次续上 demon 链。
4. 这与“跳一次后一直在自动跳跃”的现象完全对得上。

## 26. 2026-05-09 增量记录（v23.49）

### 26.1 本轮实际代码落点

本轮只继续改了 runtime 两处：

1. `src/runtime/dllmain_section_runtime_hooks.inl`
   - 在 `ClearMountedDemonJumpTransientRuntimeState(...)` 中新增 gate probe 清理：
     - `g_recentMountedDemonJumpGateProbeItemId`
     - `g_recentMountedDemonJumpGateProbeChildSkillId`
     - `g_recentMountedDemonJumpGateProbeTick`
   - 同时把 clear 日志扩成：
     - `hadProbe`
     - `probeChild`
     - `probeAge`
   - 目的：直接验证 postpacket/pass 清理时是否真的把旧 probe 一起带掉。
2. `src/dllmain.cpp`
   - build marker 更新为：
     - `v23.49-2026-05-09-mounted-clear-gate-probe-after-release`

### 26.2 当前构建证据

1. 新 build marker：
   - `v23.49-2026-05-09-mounted-clear-gate-probe-after-release`
2. 新编译产物 SHA256：
   - `45A34B05941CF285901C87BBFBC5C905BC3E698CAD50D0907BB123DE71D992D8`

### 26.3 当前部署状态

本轮编译成功，但**运行目录 DLL 还没有覆盖成功**。

原因是：

- `G:\code\mxd\Data\Plugins\SS\SS.dll` 当前仍被进程占用

本轮覆盖时报错：

- `The process cannot access the file 'G:\code\mxd\Data\Plugins\SS\SS.dll' because it is being used by another process.`

当前运行目录 DLL SHA256 仍是旧版：

- `129DF035725AF381F3CF2327561D55F9EF4D6C38D4B4D801201FCA2E3B7D6DF1`

也就是：

- 运行中的仍然是 `v23.48`
- 不是 `v23.49`

### 26.4 v23.49 下一轮必须验证的日志点

关闭客户端后完成覆盖，再启动验证时必须先看：

1. `[Build] marker=v23.49-2026-05-09-mounted-clear-gate-probe-after-release`
2. `B28A00-up-postpacket` 或 `433380-pass` 清理日志里是否出现：
   - `hadProbe=1`
   - 且随后不再被旧 probe 复燃
3. 在一次普通跳或一次 `↑↑` 结束后，是否不再立刻出现：
   - 清理后又马上被 `7DBC50` fresh-child 续回的现象

### 26.5 当前结论

本轮不是再试一个泛化猜测，而是把 `v23.48` 日志里“intent/route 已清，但 fresh-child 还能继续复燃”的具体缺口补上：

1. `v23.48` 的问题不是 postpacket 完全没清。
2. 问题是 `gate probe` 还活着。
3. `v23.49` 就是针对这个残留状态做的窄修补。
4. 但在运行目录 DLL 真正覆盖前，不能把 `v23.49` 当成已上线结果。

## 27. 2026-05-09 增量记录（v23.49 运行验证）

### 27.1 运行证据

用户本轮已经实际跑到：

- `[Build] marker=v23.49-2026-05-09-mounted-clear-gate-probe-after-release`

说明：

- `v23.49` 已经成功进入运行环境
- 当前“还是会自动无限跳跃”不是旧版残留体感

### 27.2 v23.49 日志确认出的新结论

`gate probe` 清理虽然落地了，但**无限跳根因并不只在 probe**。

关键日志链如下：

1. `433380 pass clear` 后已经进入“根上下文清空”的状态：
   - `root=0 current=0`
   - `primed=0`
2. 随后 `433380 terminal keep` 仍然记录：
   - `lateLock=0 recent=1 probe=0`
3. 紧接着又出现：
   - `level keep child query=30010183`
   - `preserve-gate intent-refresh=1`
   - `reason=7DBC50 ... primed=1`

对应日志样本：

- `[MountDemonJumpContext] 433380 pass clear caller=0x05E3FC8E ... mount=1932065 configured=30010110 recent=1 primed=0 root=0 current=0`
- `[MountDemonJumpContext] 433380 terminal keep caller=0x05E3FC8E mount=1932065 root=0 current=0 lateLock=0 recent=1 probe=0`
- `[MountDemonJumpLevelSeed] 7DBC50 caller=0x00433B4C ... preserve-gate intent-refresh=1`
- `[MountDemonJumpPrime] reason=7DBC50 mount=1932065 ... root=30010110 current=30010183 primed=1`

### 27.3 根因修正

这说明：

1. `v23.49` 里即使 `probe` 已清掉，
2. 只要 `recent intent` 还在，
3. `433380 terminal keep` 就会继续保留 demon transient，
4. 接着 `7DBC50` fresh-child 再次把 mounted demon 链点燃，
5. 最终表现就是自动无限跳跃。

也就是说：

- 真正错误的是 `433380 terminal keep` 把“recent-only”也当成了 keep 条件。

## 28. 2026-05-09 增量记录（v23.50）

### 28.1 本轮实际代码落点

本轮只改了一个终态判断：

- 文件：
  - `src/runtime/dllmain_section_runtime_hooks.inl`
- 位置：
  - `433380 terminal clear / terminal keep` 判定

修改前：

1. 只有在：
   - `!contextStillActive`
   - `!lateLocalLockStillActive`
   - `!hasFreshTerminalIntent`
   - `!hasFreshTerminalProbe`
2. 才会进入 terminal clear

这意味着：

- `recent=1 probe=0 context=0 lock=0` 仍会被 keep

修改后：

1. 只要：
   - `!contextStillActive`
   - `!lateLocalLockStillActive`
   - `!hasFreshTerminalProbe`
2. 就直接 terminal clear

也就是：

- 没有 live context
- 没有落地锁
- 没有 fresh probe

时，`recent-only` 不再单独保留 demon transient。

同时把 terminal clear 日志补成：

- `recent=%d probe=%d`

方便后续直接对照。

### 28.2 当前构建证据

1. 新 build marker：
   - `v23.50-2026-05-09-mounted-terminal-clear-ignore-recent-only`
2. 新编译产物 SHA256：
   - `C3BA600736E2929CB16E6F60928F95F63CA25FE068F5BCDFA98FFF47B258D25C`

### 28.3 当前部署状态

本轮编译成功，但**运行目录 DLL 还未覆盖成功**。

原因：

- `G:\code\mxd\Data\Plugins\SS\SS.dll` 仍被进程占用

覆盖报错：

- `The process cannot access the file 'G:\code\mxd\Data\Plugins\SS\SS.dll' because it is being used by another process.`

运行目录当前仍是旧版哈希：

- `45A34B05941CF285901C87BBFBC5C905BC3E698CAD50D0907BB123DE71D992D8`

即：

- 运行中的仍然是 `v23.49`
- 不是 `v23.50`

### 28.4 当前结论

本轮结论已经从日志上进一步收紧：

1. `v23.49` 证明了 probe 不是最后一个残留点。
2. 真正把无限跳保留下来的，是 `433380 terminal keep` 对 `recent-only` 的错误保留。
3. `v23.50` 就是只针对这个判定做的窄修补。
4. 在运行目录 DLL 真正切到 `v23.50` 前，不能拿当前体感继续判断这轮代码效果。

## 29. 2026-05-09 增量记录（v23.50 运行验证）

### 29.1 运行证据

用户本轮已经实际跑到：

- `[Build] marker=v23.50-2026-05-09-mounted-terminal-clear-ignore-recent-only`

同时运行目录哈希也已对应：

- `C3BA600736E2929CB16E6F60928F95F63CA25FE068F5BCDFA98FFF47B258D25C`

### 29.2 v23.50 硬结论

`433380-pass-terminal` 的 recent-only clear 已经真正生效，但**自动跳依然存在**。

关键日志链：

1. terminal clear 已执行：
   - `[MountDemonJumpContext] 433380 terminal clear ... root=0 current=0 lateLock=0 recent=1 probe=0`
   - `[MountDemonJump] clear runtime transient state mount=1932065 reason=433380-pass-terminal hadProbe=0 probeChild=0 probeAge=0`
2. 但 clear 后下一拍立刻又出现：
   - `[MountDemonJump] level keep child query=30010183`
   - `[MountDemonJump] 7DBC50 fallback query=30010183 lookup=30010183 flags=0 mount=1932065 -> result=1`
   - `[MountDemonJumpPrime] reason=7DBC50 ... root=30010110 current=30010110 primed=1`

结论：

1. `v23.50` 已经不是“terminal keep 没清掉 recent-only”。
2. 真正的复燃点变成了：
   - `7DBC50` 的 mounted fallback 仍然会在 clear 后继续放行 `30010183`
3. 也就是：
   - 清理链已经执行
   - 但 level/gate fallback 还在重新点火

## 30. 2026-05-09 增量记录（v23.51）

### 30.1 本轮实际代码落点

本轮没有再碰 `433380` 清理链，而是直接收紧 mounted demon 的 fallback 放行条件。

修改文件：

- `src/runtime/dllmain_section_runtime_hooks.inl`

修改点：

- `ResolveMountedRuntimeSkillGateAllow(...)`

新增 demon 专用窄校验：

1. 当 kind=`MountedRuntimeSkillKind_DemonJump`
2. 且请求的是 demon root/child 相关 skill
3. 必须至少满足以下之一，才允许 mounted fallback：
   - `HasRecentMountedDemonJumpIntent(...)`
   - `TryGetRecentMountedDemonJumpGateProbeMountItemId(...)`
   - 当前 demon context 仍 active

否则直接拒绝 fallback，并打日志：

- `[MountDemonJumpGate] reject fallback original=%d mapped=%d mount=%d recent=%d probe=%d root=%d current=%d`

目的：

- 直接拦掉 `v23.50` 里这条清理后仍然发生的复燃链：
  - `terminal clear`
  - `7DBC50 fallback query=30010183 -> result=1`
  - `reason=7DBC50 primed=1`

### 30.2 当前构建与部署证据

1. 新 build marker：
   - `v23.51-2026-05-09-mounted-gate-fallback-recent-probe-context`
2. 新编译产物 SHA256：
   - `D8023E264A6E0AA8F5981EB0932F7614CD383463E2F5E3BF8370F4533EFD91B9`
3. 运行目录 DLL SHA256：
   - `D8023E264A6E0AA8F5981EB0932F7614CD383463E2F5E3BF8370F4533EFD91B9`

即：

- `build\Debug\SS.dll`
- `G:\code\mxd\Data\Plugins\SS\SS.dll`

已经完成哈希一致的部署。

### 30.3 下一轮验证重点

优先确认三件事：

1. 日志 marker 是否为：
   - `v23.51-2026-05-09-mounted-gate-fallback-recent-probe-context`
2. 自动跳出现时，是否开始出现：
   - `[MountDemonJumpGate] reject fallback ...`
3. clear 后是否还会继续出现：
   - `7DBC50 fallback query=30010183 -> result=1`

### 30.4 当前结论

本轮修补不再处理“清理有没有成功”，而是直接针对清理后的错误放行：

1. `v23.50` 已证明 clear 链是执行了的。
2. 后续仍自动跳，是因为 fallback gate 继续放行 mounted demon child。
3. `v23.51` 就是把这个 fallback gate 收窄到必须存在 recent/probe/context 才能通过。

## 31. 2026-05-09 增量记录（v23.52）

### 31.1 本轮问题证据

最新 `C:\SuperSkillWnd.log` 已经证明：

1. `v23.51` 能拦住 child fallback：
   - `[MountDemonJumpGate] reject fallback original=30010183 mapped=30010183 ...`
2. 但 `433380 terminal clear` 之后仍然会被残余帧重新点火：
   - `[MountDemonJumpContext] 433380 terminal clear ... root=0 current=0 lateLock=0 recent=1 probe=0`
   - `[MountDemonJump] clear runtime transient state mount=1932065 reason=433380-pass-terminal ...`
   - 随后再次出现：
     - `[MountDemonJump] 7DBC50 fallback query=30010110 lookup=30010110 ... -> result=1`
     - `[MountDemonJumpProbe] B844D0 early-up seed ... armIntent=1 prime=1 root=30010110 current=30010183`
3. 也就是：
   - 不是 `433380` 没清
   - 不是 child fallback 还在放
   - 而是 terminal clear 后的残余 native frame 又把 root/probe/context 重新武装

### 31.2 本轮代码修改

本轮只做一个窄修正：给 `433380 terminal clear` 增加短抑制窗，专门拦截 clear 之后的残余重入。

修改文件：

- `src/runtime/dllmain_section_runtime_hooks.inl`
- `src/dllmain.cpp`

具体落点：

1. 新增 terminal-clear 记录：
   - `ObserveMountedDemonJumpTerminalClear(...)`
   - `HasRecentMountedDemonJumpTerminalClear(...)`
2. `433380-pass-terminal` 真正 clear 前先记录：
   - `[MountDemonJumpClear] arm terminal-clear mount=%d reason=433380-pass-terminal`
3. `7DBC50` demon gate 对 root fallback 追加冷却拒绝：
   - `[MountDemonJumpGate] reject terminal-clear original=%d mapped=%d ...`
4. `B844D0` early-up seed 在冷却窗内不再重新点火：
   - `[MountDemonJumpProbe] B844D0 suppress terminal-clear mount=%d ...`
5. 一旦真实 demon intent 再次被观测到：
   - `ObserveMountedDemonJumpIntent(...)`
   会主动清掉这段 terminal-clear 抑制窗，避免影响下一次真实触发

### 31.3 当前构建证据

1. 新 build marker：
   - `v23.52-2026-05-09-mounted-terminal-clear-cooldown-guard`
2. 新编译产物 SHA256：
   - `37A3D1A23BF3DF211ECACCE950A4A1762A67F732B51BB9D0985119D6DB163759`

### 31.4 当前部署状态

编译已成功，但运行目录 DLL 在记录时仍被 `Maplestory` 占用：

1. 当前运行目录哈希仍是旧版：
   - `D8023E264A6E0AA8F5981EB0932F7614CD383463E2F5E3BF8370F4533EFD91B9`
2. 已启动后台自动部署助手：
   - `tools/deploy_ss_after_unlock.ps1`
   - 后台进程 PID：`10960`
3. 用户关闭游戏后，会自动覆盖：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`

### 31.5 下一轮验证点

用户重启后优先只看这几类日志：

1. marker 是否已切到：
   - `v23.52-2026-05-09-mounted-terminal-clear-cooldown-guard`
2. 自动跳场景里是否出现：
   - `[MountDemonJumpProbe] B844D0 suppress terminal-clear ...`
   - `[MountDemonJumpGate] reject terminal-clear ...`
3. `433380 terminal clear` 之后，是否还会再次跟出：
   - `B844D0 early-up seed ... prime=1`
   - `7DBC50 fallback query=30010110 ... -> result=1`

## 32. 2026-05-09 运行验证补记（v23.52 尚未真正进入运行时）

### 32.1 当前硬证据

用户反馈“还是自动跳”后，立即核对运行态证据，结果如下：

1. 运行目录 DLL 哈希仍是：
   - `D8023E264A6E0AA8F5981EB0932F7614CD383463E2F5E3BF8370F4533EFD91B9`
2. 当前日志 marker 仍是：
   - `v23.51-2026-05-09-mounted-gate-fallback-recent-probe-context`
3. `C:\SuperSkillWnd.log` 里仍然只有旧链：
   - `7DBC50 fallback query=30010110 ... -> result=1`
   - `B844D0 early-up seed ...`
   - `433380 terminal clear ...`
4. 没有出现 `v23.52` 新增日志：
   - `[MountDemonJumpClear] arm terminal-clear ...`
   - `[MountDemonJumpGate] reject terminal-clear ...`
   - `[MountDemonJumpProbe] B844D0 suppress terminal-clear ...`

结论：

这次“还是自动跳”的测试结果，实际仍然是在 `v23.51` 上得到的，**不能用于判断 `v23.52` 是否有效**。

### 32.2 当前部署现场

核对时进程状态如下：

1. `Maplestory.exe` 仍在运行
   - StartTime：`2026-05-09 18:46:26`
2. 后台部署助手仍在等待
   - PID：`10960`
   - StartTime：`2026-05-09 18:53:33`
3. 新编译产物仍是：
   - marker：`v23.52-2026-05-09-mounted-terminal-clear-cooldown-guard`
   - SHA256：`37A3D1A23BF3DF211ECACCE950A4A1762A67F732B51BB9D0985119D6DB163759`

即：

1. 代码已经编译好
2. 自动部署助手已经挂起等待
3. 但用户本轮测试时，运行中的依然是旧版 `v23.51`

## 33. 2026-05-09 增量记录（v23.53）

### 33.1 v23.52 运行后的新结论

`v23.52` 已经证明：

1. terminal clear 标记真的被写入了：
   - `[MountDemonJumpClear] arm terminal-clear mount=1932065 reason=433380-pass-terminal`
2. 但它不是立刻失效，而是**短超时过早**：
   - clear 之后先经历一段 `B844D0 state=0 h=0`
   - 之后同一段残余链才进入：
     - `B844D0 early-up seed`
     - `7DBC50 fallback query=30010110 -> result=1`
3. 也就是说：
   - `v23.52` 的问题不再是“没打标”
   - 而是“只靠 160ms 超时太短，残余链晚一点才回来”

### 33.2 本轮修正

本轮把 terminal-clear 从“短时间抑制”改成“直到下一次真实 gate 再释放”。

修改文件：

- `src/runtime/dllmain_section_runtime_hooks.inl`
- `src/dllmain.cpp`

具体修改：

1. `kMountedDemonJumpTerminalClearSuppressMs`
   - `160 -> 1500`
2. 新增：
   - `ClearMountedDemonJumpTerminalClear(...)`
3. 释放时机改成显式 gate：
   - `42DE20`
   - `42DE20-no-intent`
   - `B22630-raw-up-commit`
4. root fallback 收紧：
   - 只要 terminal-clear 还有效，且当前没有 active context，
   - `30010110` root fallback 即使带着 probe 也拒绝放行
5. `B844D0-up-seed`
   - 不再作为 terminal-clear 的释放条件
   - 避免 residual `up-seed` 自己把自己重新点燃

### 33.3 当前构建与部署证据

1. build marker：
   - `v23.53-2026-05-09-mounted-terminal-clear-sticky-until-real-gate`
2. `build\Debug\SS.dll` SHA256：
   - `42926E4F3A7AC0133E01087C8E3B800AF013DB432071C28A69D5695603F5F1D5`
3. `G:\code\mxd\Data\Plugins\SS\SS.dll` SHA256：
   - `42926E4F3A7AC0133E01087C8E3B800AF013DB432071C28A69D5695603F5F1D5`

结论：

本轮已经完成编译并直接覆盖到运行目录，下一次启动就是 `v23.53`。

### 33.4 下一轮验证点

用户下一次启动后，只看下面三类证据：

1. marker：
   - `v23.53-2026-05-09-mounted-terminal-clear-sticky-until-real-gate`
2. clear 后是否开始出现：
   - `[MountDemonJumpClear] release terminal-clear ...`
   - 且 release 原因应该是：
     - `42DE20`
     - `42DE20-no-intent`
     - `B22630-raw-up-commit`
3. 自动跳场景里是否还会出现：
   - `B844D0 early-up seed ...`
   - `7DBC50 fallback query=30010110 ... -> result=1`

如果 `v23.53` 正常生效，残余自动跳阶段应该优先出现：

- `[MountDemonJumpProbe] B844D0 suppress terminal-clear ...`
或
- `[MountDemonJumpGate] reject terminal-clear ...`

## 34. 2026-05-09 增量记录（v23.54 / v23.55）

### 34.1 v23.53 旧日志已确认的问题点

用户本轮重新提供的两份日志：

- `C:\SuperSkillWnd首次跳跃会多跳一次.log`
- `C:\SuperSkillWnd只有第二次↑↑成功一次，后续全部无效.log`

这两份日志都不是当前新版本，而是旧的：

- `[Build] marker=v23.53-2026-05-09-mounted-terminal-clear-sticky-until-real-gate`

从这两份旧日志里，已经稳定确认两件事：

1. `terminal-clear` 的错误释放点确实存在：
   - `release terminal-clear ... reason=42DE20`
   - `release terminal-clear ... reason=B2F370-arm-missing-intent`
2. 除了 `terminal-clear` 之外，还存在另一条读口过宽问题：
   - 同一次起跳里，`[MountDemonJumpContext] promote fresh child ... -> child=30010183`
   - 在 `SuperSkillWnd首次跳跃会多跳一次.log` 中共出现 `48` 次
   - 并且紧跟着大量
     `7DBC50 ... recentMatch=1 intent=1 root=30010110 current=30010183 context=1 prime-up=0`

结论：

旧版问题不只是“terminal-clear 释放过早”，还有
“`7DBC50` fresh-child 等级读口反复把 root context 虚拟提升成 child context”。

### 34.2 v23.54 已完成的修正

本轮先落地了 `v23.54`，只修 `terminal-clear` 的释放边界：

1. 新增 `ShouldReleaseMountedDemonJumpTerminalClearForReason(...)`
2. 只允许下面这些 reason 真正释放 terminal-clear：
   - `B22630-raw-up-commit`
   - `AFB710-post-context`
   - `B22630-late-child-*`
3. 明确不再允许下面这些 residual reason 释放：
   - `42DE20`
   - `42DE20-no-intent`
   - `B2F370-arm-missing-intent`
   - 泛化的 `skill-level-fresh-child`

`v23.54` 构建 / 部署证据：

1. marker：
   - `v23.54-2026-05-09-mounted-terminal-clear-real-release-only`
2. SHA256：
   - `525CBFC5E78DCD57BB4E2E4BAB6BE6B1EA3D008B83603E9A94CE07866F48A0AE`

### 34.3 v23.55 新增的窄修正

基于上面的旧日志证据，本轮继续做了一个**比 terminal-clear 更窄**的修正：

目标：

- 不全局改 mounted demon jump context
- 只收紧 `7DBC50` 这条 fresh-child 等级读口
- 避免它在“本来只该 refresh intent”的分支里，反复读到 `effective context`
  并把 `30010110/30010110` 虚拟解释成 `30010110/30010183`

代码修改：

1. 文件：
   - `src/runtime/dllmain_section_runtime_hooks.inl`
   - `src/dllmain.cpp`
2. 变更：
   - 在 `7DBC50` 且 caller 为
     `0x00433B4C / 0x00A04B7A / 0x00AE0479`
     的 fresh-child 读口上，
     `TryReadMountedDemonJumpEffectiveContextState(...)`
     改为只读
     `TryReadMountedDemonJumpContextState(...)`
   - 也就是：
     `7DBC50` 这条路现在看 `raw context`，不再吃到 read-side 的虚拟 child promote
3. 新日志字段：
   - `ctxRead=raw`
   - `ctxRead=effective`

设计边界：

1. 只限制 `7DBC50` 指定 caller 的上下文读取方式
2. 不改：
   - `B844D0-up-seed`
   - `B22630`
   - `AFB710`
   - `B300AC`
   - 其他 mounted demon jump / mounted double-jump 路线

### 34.4 v23.55 构建 / 部署证据

1. marker：
   - `v23.55-2026-05-09-mounted-7dbc50-raw-context-only`
2. `build\Debug\SS.dll` SHA256：
   - `0C0754F516A72D0CE3E80CE496FABF98F062EA0086DDB7E81928ECDD90F09554`
3. `G:\code\mxd\Data\Plugins\SS\SS.dll` SHA256：
   - `0C0754F516A72D0CE3E80CE496FABF98F062EA0086DDB7E81928ECDD90F09554`

结论：

`v23.55` 已编译并覆盖到运行目录，下一次启动游戏时运行的就是这版。

### 34.5 下一轮最小验证点

只需要做最小复测：

1. 骑宠 `1932065`
2. 只测一次：
   - 普通起跳
   - 空中 `↑↑`
3. 看 `C:\SuperSkillWnd.log`

优先观察：

1. marker 是否为：
   - `v23.55-2026-05-09-mounted-7dbc50-raw-context-only`
2. `7DBC50` 日志里是否改成：
   - `ctxRead=raw`
3. 是否不再出现旧版那种连续几十条：
   - `promote fresh child ... -> child=30010183`
4. 体感上：
   - 首次跳跃不应再额外多跳一次
   - `↑↑` 不应再只在“第二次”才成功

## 35. 2026-05-09 增量记录（v23.56）

### 35.1 v23.55 运行后的新结论

`v23.55` 当前日志已经证明：

1. 运行版本正确：
   - `[Build] marker=v23.55-2026-05-09-mounted-7dbc50-raw-context-only`
2. 旧的 `promote fresh child` 连刷问题已经不再作为主现象出现。
3. 但 `7DBC50` 收得过宽，连主 child seed 读口也一起被收掉了：
   - `query=30010183 lookup=30010183 ... caller=0x00433B4C`
   - `reject fallback original=30010183 mapped=30010183 mount=1932065 recent=0 probe=0 root=0 current=0`
   - 后续 `MountDemonJumpSeed ... primed=0 root=0 current=0`
4. 同时日志出现：
   - `nativeLevel=0`
   - `skip eager intent mount=1932065`

结论：

`v23.55` 不是“没部署”，而是把 `7DBC50` 的 `raw context` 限制开得太大，
把本来还需要保留 `effective context` 的 `00433B4C` 首次 child seed 读口也误伤了。

### 35.2 v23.56 修正

本轮继续收窄边界：

1. 只让 `7DBC50` 的 `00A04B7A` support lookup caller 走 `raw context`
2. 恢复：
   - `0x00433B4C`
   - `0x00AE0479`
   继续使用 `effective context`

目的：

1. 保留 `00433B4C` 首次 `30010183` child seed 的正常能力
2. 继续压住旧日志里真正刷屏的
   `00A04B7A (80001099 / 2301014 / 80001095 / 30001902)` support lookup

### 35.3 构建与部署状态

1. marker：
   - `v23.56-2026-05-09-mounted-7dbc50-raw-a04b7a-only`
2. 新构建 `build\Debug\SS.dll` SHA256：
   - `9F1AC9DE399641446A984A4B0743BE9817838F92186EAF1B1DA49D4A82EF97B8`
3. 用户记录时 `Maplestory` 仍在运行：
   - 进程已占用 `G:\code\mxd\Data\Plugins\SS\SS.dll`
4. 因此运行目录当时仍是旧版 `v23.55`：
   - `0C0754F516A72D0CE3E80CE496FABF98F062EA0086DDB7E81928ECDD90F09554`
5. 已重新启动延迟部署助手：
   - `tools\deploy_ss_after_unlock.ps1`

### 35.4 下一轮验证点

下一次必须先让 `v23.56` 真正覆盖完成，再测试：

1. 先关闭游戏，让部署助手完成覆盖
2. 再启动游戏
3. 只测骑宠 `1932065` 的一次 `↑↑`

验证时优先看：

1. marker 是否为：
   - `v23.56-2026-05-09-mounted-7dbc50-raw-a04b7a-only`
2. `00433B4C` 的 `30010183` query 是否恢复 seed
3. `00A04B7A` support lookup 是否仍不再反复制造 child context 噪音

## 36. 2026-05-09 增量记录（v23.57 / v23.58）

### 36.1 v23.57 证据结论

本轮先重新核对了用户提供的原生日志与当前运行日志：

1. 原生职业 `3001` 的 `↑↑` 正常链为：
   - `7DBC50 caller=0x00AE0479 query=30010110 ... final=1`
   - `7DBC50 caller=0x00433B4C query=30010183 ... final=1`
   - `B22630 enter a2=18 ...`
   - `B22630 enter a2=38 ... leave result=4 ... current=30010183`
   - `AFB710 observe native path`
   - `B2F370 observe native release mount=... skill=30010183`
   - `B300AC reroute demon skill=30010183 -> full-release`

2. `v23.56` 的 mounted `↑↑` 失败样本里，下降清状态链存在一个误清风险：
   - `B1DB10` 的 descending clear 之前读取的是 effective context
   - recent-child cache 可能把 live `30010110/30010110` 伪装成 `30010110/30010183`
   - 结果是第一次 mounted `↑↑` 还没走完整个 native chain，就可能被 `B1DB10-descending-up` 提前清掉

3. 因此 `v23.57` 做了一个非常窄的修正：
   - `hkMountedDemonJumpMoveB1DB10`
   - descending clear 改为只看 `TryReadMountedDemonJumpContextState(...)`
   - 不再用 effective context 参与这一段清理

4. `v23.57` 构建 marker：
   - `v23.57-2026-05-09-mounted-b1db10-desc-live-context-only`

5. `v23.57` 构建/部署哈希：
   - `build\Debug\SS.dll`
   - `F4B4DE03946C9C4F2289FBE9CB5ADA3F454458E0AD37CF55E0DDDF3EA4D4962F`

### 36.2 v23.57 新日志暴露出的真实问题

重新读取当前最新 `C:\SuperSkillWnd.log` 后，证据表明失败主因已经不再是 `B1DB10` 首次误清，而是“terminal-clear 残留拦截”：

1. 首轮成功样本中，mounted `↑↑` 的 seed 仍然可以成立：
   - `marker=v23.57-2026-05-09-mounted-b1db10-desc-live-context-only`
   - `7DBC50 caller=0x00AE0479 query=30010110 ... final=1`
   - `7DBC50 caller=0x00433B4C query=30010183 ... final=1`
   - `MountDemonJumpSeed result ... primed=1 root=30010110 current=30010183`

2. 但后续失败样本里反复出现：
   - `42DE20 demon no-intent fallback`
   - `7DBC50 reject terminal-clear original=30010110 mapped=30010110 mount=1932065 recent=1 probe=0 root=0 current=0`
   - `433380 terminal clear caller=... recent=1 probe=0`
   - `clear runtime transient state ... reason=433380-pass-terminal`
   - 随后 `7DBC50 caller=0x00433B4C query=30010183 ... final=0`
   - `MountDemonJumpSeed ... primed=0 root=0 current=0`

3. 这说明：
   - 成功后留下的 `terminal-clear` 抑制位仍在
   - 下一次新的 mounted demon-jump 虽然已经重新 arm 了 recent intent
   - 但在 fresh gate probe 还未来得及重新建立前，`7DBC50` 已被 `terminal-clear` 直接拒掉
   - 同时 `433380` 还会在 `recent=1 probe=0` 的窗口里继续重新 arm `terminal-clear`

4. 结论：
   - 当前失败核心不是“没有意图”
   - 而是“上一轮 terminal-clear 对下一轮 fresh retrigger 的误伤”

### 36.3 v23.58 修正

基于上面的日志证据，`v23.58` 只改了两处窄逻辑：

1. `433380` terminal clear 条件收窄
   - 只有在：
     - `!contextStillActive`
     - `!lateLocalLockStillActive`
     - `!hasFreshTerminalIntent`
     - `!hasFreshTerminalProbe`
     时才允许重新 arm `433380-pass-terminal`
   - 含义：
     - 若新一轮 mounted demon-jump 已经重新出现 fresh intent，即使 probe 还没到，也不能再把 terminal-clear 重挂上去

2. `7DBC50` terminal-clear 拒绝条件收窄
   - 只有在：
     - `hasRecentTerminalClear`
     - `!contextActive`
     - `!hasRecentIntent`
     - `!hasFreshProbe`
     时才允许 `reject terminal-clear`
   - 含义：
     - 旧 terminal-clear 只能拦“真的没有新一轮触发”的情况
     - 一旦已经有 fresh intent 或 fresh probe，就必须让 root `30010110` lookup 继续进入下一轮 seed

3. `v23.58` marker：
   - `v23.58-2026-05-09-mounted-terminal-clear-retrigger-gate`

### 36.4 v23.58 构建与部署证据

1. 新构建产物 marker 已确认写入：
   - `build\Debug\SS.dll` 二进制内可检出
   - `v23.58-2026-05-09-mounted-terminal-clear-retrigger-gate`

2. 新构建产物 SHA256：
   - `9986C7A7831A32D34299CB7B9024114D44D0574A06EFF9E69320159C659465F4`

3. 运行目录旧 DLL 当时仍被游戏占用，尚未覆盖：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   - marker 仍为 `v23.57-2026-05-09-mounted-b1db10-desc-live-context-only`
   - SHA256：
   - `F4B4DE03946C9C4F2289FBE9CB5ADA3F454458E0AD37CF55E0DDDF3EA4D4962F`

4. 因此在用户真正关闭游戏前：
   - “源码已改 + 新 DLL 已编译”
   - 但“运行时尚未切到 v23.58”

### 36.5 下一轮验证要求

必须先确认运行目录 DLL 已切到 `v23.58`，再测。

验证顺序：

1. 关闭游戏，释放：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
2. 完成覆盖后再启动游戏
3. 只测骑宠 `1932065` 的：
   - `↑↑`

验证日志优先看：

1. marker 是否为：
   - `v23.58-2026-05-09-mounted-terminal-clear-retrigger-gate`
2. 失败轮里是否还出现：
   - `7DBC50 reject terminal-clear ... recent=1 probe=0`
3. `433380` 是否还会在 fresh recent intent 窗口里继续：
   - `terminal clear ... recent=1 probe=0`
4. `30010110 -> 30010183` 的 seed 是否恢复为可重复进入，而不是“成功一次后多次失败”

### 36.6 2026-05-09 20:xx 当前“↑↑无效”回报的版本判定

本轮用户回报“↑↑无效”后，先做了版本/哈希/进程三重核对，结论是：

1. 当前运行中的游戏仍在占用旧版运行时 DLL：
   - 进程：
   - `Maplestory`
   - PID：
   - `48420`
   - 启动时间：
   - `2026-05-09 20:19:51`

2. 运行目录 DLL 仍是旧版 `v23.57`：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   - `LastWriteTime=2026-05-09 20:19:08`
   - SHA256：
   - `F4B4DE03946C9C4F2289FBE9CB5ADA3F454458E0AD37CF55E0DDDF3EA4D4962F`

3. 构建目录里的新 DLL 已经是 `v23.58`，但尚未真正部署进运行目录：
   - `G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
   - `LastWriteTime=2026-05-09 20:30:18`
   - SHA256：
   - `9986C7A7831A32D34299CB7B9024114D44D0574A06EFF9E69320159C659465F4`

4. 当前 `C:\SuperSkillWnd.log` 明确仍在输出旧 marker：
   - `[Build] marker=v23.57-2026-05-09-mounted-b1db10-desc-live-context-only`

5. 当前日志里仍可看到旧版失败特征，说明这次“↑↑无效”不是 `v23.58` 结果：
   - `reject terminal-clear original=30010110 mapped=30010110 mount=1932065 recent=1 probe=0 root=0 current=0`
   - `433380 terminal clear caller=... recent=1 probe=0`
   - `42DE20-no-intent`

6. 因此本轮不能把“↑↑无效”归因到 `v23.58` 逻辑本身。
   - 先决条件仍然是：
   - 完整退出游戏
   - 让 `SS.dll` 从旧哈希切到新哈希
   - 再用只测一次 `↑↑` 的新日志重新判断

## 37. 2026-05-09 增量记录（v23.60）

### 37.1 本轮问题来源

基于用户当前最新描述：

1. `下 + 跳跃` 仍然会跳起，不应该触发起跳。
2. `↑↑ + 跳跃` 仍然经常按不出骑宠恶魔跳跃。
3. 滑翔在放开跳跃后仍可能继续滑，而不是“按住期间有效，放手立即收”。

同时，本轮重新核对 `C:\SuperSkillWnd.log` 后又确认了一条关键事实：

1. 用户那轮日志实际仍在旧运行时：
   - `[Build] marker=v23.58-2026-05-09-mounted-terminal-clear-retrigger-gate`
2. 旧日志里仍能直接看到：
   - mounted `↑↑` 被劫持进 `30010186`
   - `B844D0` 在 `h=1/h=2` 阶段继续过早 arm up seed
   - glide 去重仍使用长窗口并会拖住后续输入

### 37.2 本轮修点

本轮只做 3 个有日志/知识库证据支撑的窄修正：

1. `B844D0` 早期 up-seed 收紧：
   - 新增 `downLatch == 0` 限制
   - 目标是阻止 `下 + 跳跃` 被误武装成 mounted demon up seed

2. `TryForceFinalizeMountedDemonJumpNativeActionTailState(...)`
   - 补回 `exact362`
   - 条件：
     - `afb == 362`
     - `state1172 == -1`
     - `state1192 == 362`
   - 这样 glide 的 native tail 不再只接受 `353/354`

3. `B1DB10 / B1C9E0` 晚收尾补回：
   - `30010184`：
     - `B1DB10-side-late-finalize`
     - `B1C9E0-side-late-finalize`
   - `30010186`：
     - `B1DB10-glide-late-finalize`
     - `B1C9E0-glide-late-finalize`
   - 目标是让 side/glide 不再只靠更晚的 terminal 路径兜底收尾

4. glide 去重窗口恢复为“去重”和“持续”分离：
   - `kMountedDemonJumpGlidePacketSustainMaxAgeMs = 600`
   - `kMountedDemonJumpRepeatedGlidePacketSuppressMaxAgeMs = 80`
   - `B28A00 suppress repeated glide` 分支里不再重新 `RememberMountedDemonJumpGlidePacket(...)`
   - 目标是避免放手后 glide 自己继续把自己续命

### 37.3 构建与哈希证据

1. 新 build marker：
   - `v23.60-2026-05-09-mounted-down-block-glide-tail-hold-fix`

2. 构建产物与运行目录最终 SHA256：
   - `602728DD7AD3813BAE4E9911D23D3A4C2FFAFEBF0771CB529ABB15818E673CE0`

3. 本轮 `certutil -hashfile` 已确认：
   - `G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   两者哈希一致

4. 二进制内 marker 也已确认写入：
   - `v23.60-2026-05-09-mounted-down-block-glide-tail-hold-fix`

### 37.4 下一轮最小验证点

用户重启游戏后，只需要做这 3 轮最小验证：

1. 骑宠 `1932065`
2. 只测：
   - `下 + 跳跃`
   - 空中 `↑↑`
   - 空中按住跳跃滑翔，随后放开跳跃

优先看证据：

1. `C:\SuperSkillWnd.log` marker 是否为：
   - `v23.60-2026-05-09-mounted-down-block-glide-tail-hold-fix`
2. `下 + 跳跃` 时，是否不再出现新的 `B844D0-up-seed`
3. `↑↑` 时，是否不再被旧 `30010186` 路由劫持
4. 放开跳跃后，glide 是否更快退出，不再靠旧 glide packet 窗口继续续命

## 38. 2026-05-10 增量记录（v23.61）

时间：
- `2026-05-10 12:58:49`

### 38.1 本轮新增证据

基于当前最新 `C:\SuperSkillWnd.log`，又确认了 2 个新的串线点：

1. `575D60` 的 mounted up 预热范围过宽：
   - 在 `a2=18 a3=0x00380300` 这条 glide 提交签名上，也会套用
     `575D60-preseed-root-only-up`
   - 这会把 `↑↑`、按住跳跃滑翔、空中双击跳跃三条输入链混在一起

2. `TryClearMountedDemonJumpLateLocalLockState(...)` 清理时机过早：
   - 在真正的 up child `30010183` 还没有发生 native release 之前
   - 仅凭 recent child cache / probe child，就会把 `5D7C/5F2C` 提前清掉
   - 这正好命中当前失败日志里的关键窗口：
     - `a2=18 a3=0x80380000`
     - `upLatch=1`
     - 但还没进入 native `a2=38 a3=0x01480000`

### 38.2 本轮代码修正

本轮只做 3 个窄修正：

1. `hkMountedDemonJumpLateRoute575D60(...)`
   - up 预热只保留在真正的 up 签名：
     - `18 / 0x80380000`
     - `38 / 0x01480000`
     - `38 / 0x81480000`
   - 不再对 `18 / 0x00380300` 的 glide 提交做 up 预热

2. `hkMountedDemonJumpContextInputB22630(...)`
   - 给真正的 mounted up-latch 拍点
     - `a2=18 a3=0x80380000`
   - 补一层 enter-preseed
   - 解决 `575D60` 外层已经预热成 `30010183`，但一进 `B22630` 又掉回 `30010110` 的问题

3. `TryClearMountedDemonJumpLateLocalLockState(...)`
   - 对 `30010183` 新增“pre-release 不清锁”保护
   - 只有在 mounted up child 已经发生 fresh native release 后，才允许继续清 `5D7C/5F2C`
   - 目的：
     - 不再让 recent child cache 伪装成“已经完成 up child”
     - 不再在 `80380000 -> 01480000` 之间把原生后续链提前打断

### 38.3 构建 / 部署证据

1. 新 build marker：
   - `v23.61-2026-05-10-mounted-up-latch-preserve`

2. 构建目录 DLL SHA256：
   - `G:\code\c++\SuperSkillWnd\build\Debug\SS.dll`
   - `518B00F04676316829514696ABA308F6F382783A331686FCBCF590E3C37EA035`

3. 运行目录 DLL SHA256：
   - `G:\code\mxd\Data\Plugins\SS\SS.dll`
   - `518B00F04676316829514696ABA308F6F382783A331686FCBCF590E3C37EA035`

4. 结论：
   - 本轮源码、构建产物、运行时 DLL 三者已经对齐

### 38.4 下一轮最小验证点

用户重启游戏后，只做这 3 轮最小验证即可：

1. 骑宠 `1932065`
2. 只测一次空中 `↑↑`
3. 只测一次“空中按住跳跃进入滑翔，再松开跳跃”
4. 只测一次“空中双击跳跃”

优先看：

1. `C:\SuperSkillWnd.log` marker 是否为：
   - `v23.61-2026-05-10-mounted-up-latch-preserve`
2. `↑↑` 是否重新出现 native up 提交：
   - `a2=38 a3=0x01480000`
3. `0x00380300` 是否不再提前吃掉 up 预热
4. `skip local lock clear before up release` 是否命中在 `30010183` 的 pre-release 窗口
